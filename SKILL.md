---
name: ocw-to-study
description: 把「上課錄影 mp4」轉成一份 Obsidian 相容的「逐頁講解」Markdown——每一張投影片配上該段老師逐字稿（右側固定高度捲動框）與重點拆解（下方全寬）。投影片圖直接從影片畫面原生解析度擷取（保留彩色，優於黑白掃描的講義 PDF），逐字稿以 faster-whisper 轉錄後套用可累積的專有名詞校正表，段落斷點由人工審定。適用任何有錄影的課程；換一門課只要換 content.json 的課程欄位。觸發時機：使用者提供上課錄影 mp4（或指向存放影片的資料夾）並要求做逐頁筆記／講解；說 /ocw-to-study。與 handout-to-study 的分工：那個吃投影片 PDF、這個吃錄影，錄影才有老師口述。
---

# ocw-to-study — OCW 上課錄影轉逐頁講解

## 這個 skill 在解決什麼

翻轉教室的課，**老師的口述補充只存在於影片音軌**：投影片上不會寫「這個例子其實就是 TIA」「這頁我講 10 分鐘」「這個參數要靠模擬求」。而 OCW 影片常常**釋出後數週就下架**，等到期中期末複習時已經看不到。

產物是一份可長期保存、可搜尋、可掛進 Obsidian 的筆記：**投影片圖 + 老師原話 + 重點拆解**。

## 前置閘門（不滿足就停下來說明，不要硬做）

1. **要有影片檔**。使用者通常把 mp4 放在課程資料夾旁（例如 `<課程>\AIC videos\`）。沒有就問他放哪。
2. **影片必須是投影片畫面錄影**（螢幕錄製），不是講者鏡頭。純講者畫面抽不出投影片，改用 handout-to-study。
3. **確認課程資料夾**：產物寫進 `<root>/AI/CH<N>/`，筆記寫進 `<root>/`。`<root>` 通常是放講義的資料夾。
4. **先報時間**：轉錄在 **GPU 約影片長度的 1/6**（60 分鐘影片約 10 分鐘），**沒有 GPU 則約 1.5 倍**（約 90 分鐘）。無論哪種都背景執行，並先把數字告訴使用者。
5. **人工斷句的成本要先講**：步驟 5 需要通讀整份逐字稿（60 分鐘的課約 1000 段、27000 字）。這一步無法自動化，見下方說明。

## 一次做完，不要做到一半停下來

**這個 skill 一旦開跑就跑到交付為止**（2026-09-11 使用者裁示）。七個步驟是一條龍，
不是七次可以各自收工的委託。

**唯一可以停下來等使用者的理由：你需要他給資訊或做決定，而且那件事不決定就做不下去。**
（前置閘門不滿足、產物已存在要問覆蓋或接續、他的講義/影片對不上、要動到他的資料夾結構。）

**以下都不是停下來的理由**：

- 步驟 5 的通讀逐字稿很花時間 —— 那是這個 skill 的**工作內容**，不是要他批准的事項。
- 輪數變多、覺得 context 可能會滿 —— 沒有真實的 context 用量數字，不准拿這個當理由。
- 中途踩到坑要改方法 —— 自己修，修完在回報裡講一句。
- 「做完 A 了，要不要繼續做 B？」 —— 不要問，直接做 B。

進度可以隨時報，但**報告不是關卡**：講完就繼續做下一步，不要等回覆。

## 流程

腳本在 `scripts/vts.py`。`--root` 是課程資料夾，`--ch` 是章號。

```
# 1) 轉錄最耗時，先丟背景；同時做 2)、3)
python <skill>/scripts/vts.py asr      --root <課程夾> --ch 3 --video "<mp4>"   # 背景跑
python <skill>/scripts/vts.py extract  --root <課程夾> --ch 3 --video "<mp4>"
python <skill>/scripts/vts.py coverage --root <課程夾> --ch 3 --pdf "<講義.pdf>"
# 2) 轉錄完成後
python <skill>/scripts/vts.py fix      --root <課程夾> --ch 3
python <skill>/scripts/vts.py build    --root <課程夾> --ch 3
python <skill>/scripts/vts.py preview  --root <課程夾> --ch 3 --batch 5
```

**時間分配（60 分鐘影片、有 GPU）**：轉錄 ~10 分（背景）、抽圖 3~5 分、
讀逐字稿產出三樣 50~80 分、渲染 2 分，總計約 1~1.5 小時。

### 步驟 1　抽投影片（extract）

每 2 秒取樣一張低解析度圖判斷換頁（像素差異 > 1%），再用 `ffmpeg -ss` 對確定的時間點**原生解析度**重抽。產出 `AI/CH<N>/slides/` 與 `slides.json`。

**別用講義 PDF 當圖源。** 實測課程講義 PDF 是純黑白（彩度像素 0.0000%），影片畫面有 2.94% 彩色，而顏色常帶資訊（摻雜區、電場箭頭、紅色標註）。

⚠️ **例外：講義本身就是彩色的課，改用 PDF 頁，而且換頁偵測整個要換掉。**
2026-09-11 實測 DIC（吳易忠）：講義 `DIC-Lec1.pdf` 彩度 5.1%，本來就彩色；而老師會在投影片上
**即時紅筆手寫**，每一筆都讓像素差異超過 `DIFF=1.0` 的門檻 —— 2 小時的影片抽出 **524 張**
「投影片」，實際只有 26 頁。這種課的作法是：

1. 不跑 `extract` 的換頁判斷，改成**把 2 秒取樣幀逐張跟講義 PDF 的每一頁做正規化相關、取 argmax**
   （手寫只佔畫面極小比例，不影響判定；實測分數 0.92~0.99，runner-up 只有 0.55~0.60）。
   （作者另寫了一支課程專屬的 `match_pages.py`，未收錄於本 repo。）
2. 比對前**遮掉視訊會議的參與者小視窗**（DIC 是右上角，畫面寬 80%~100%、高 0%~22%）。
3. 老師來回翻頁（講 p.11 時跳回 p.10 對照）會讓時間軸非單調 → 產 `slides.json` 前先壓成單調：
   只在「第一次講到某頁」開新條目，回頭翻頁自動被前一條目吸收。
4. 老師的紅筆註記寫進「重點拆解」的文字，不要為了註記改用影片截圖。

**頁面要乾淨，雷射筆不要入鏡**（`--shots`，預設 5）。老師的雷射筆會在畫面上留一個紅點，位置隨他講到哪就跑到哪，單抽一幀就是賭那一瞬間紅點不在——賭不贏。處理分兩層：

1. **移動中的光點：多幀中位數。** 在該頁停留期間均勻抽 5 幀（頭尾各避開 1.5 秒的轉場），逐像素取中位數。光點每幀位置不同，過半數的幀在該點都是原本的顏色，中位數就把它抹掉；頁面靜止的部分每幀都一樣，中位數等於原畫面，**一個像素都不會變**。若頁面本身在動（逐項出現的動畫），任一幀與中位數的差異會超過 1%，這時自動退回單幀，不會疊出鬼影。
2. **停住不動的光點：針對性擦除。** 判準是「**雷射是疊加上去的光**」——紅光打在畫面上是加法混色，綠藍分量被抬高但不會歸零，所以光點內 `min(R,G,B)` 的中位數很高；投影片自己畫的紅色是實心純色，綠藍壓到 0。實測 CH1：雷射點 **132**、紅色項目符號 **0**。再配合面積 25~900 px、長寬比 0.4~2.5、填充率 >0.65（濾掉紅字筆畫），並且只在光點周圍一圈顏色夠均勻（標準差 ≤12）時才拿背景色填回去，旁邊有文字線條就寧可留著紅點。

實測 CH1 8 頁：s3 移動的光點與 s8 停住的光點都清掉，s7 的紅字「非誠勿試!!!」和紅色項目符號原封不動，其餘各頁整頁差異 0.000~0.209%。

⚠️ **兩個試過行不通的判準，不要再走回去**：①「中心是不是接近白色」——雷射點中心其實只是淡粉紅（min 才 144），而紅字的 bbox 中心因為框到白背景反而 min=255，兩邊剛好相反；②「中心比外圈亮」——光點是**環形**的，中心與環的 min 值只差 19。也**絕不可以把 bbox 外擴之後再看亮度**，那會框進周圍白背景，連實心紅圓都會被判成有亮心（CH1 s7 的紅色項目符號因此被誤刪過一次）。

成本：每頁 5 次 `ffmpeg -ss`，CH1 8 頁約 1 分鐘、CH2 27 頁約 3 分鐘。要關掉就 `--shots 1`。

### 步驟 2　驗證有沒有漏抓（coverage）

**判讀看對角線位移，不要看相似度分數**（黑白 vs 彩色會把分數壓得很低）。若「PDF 頁 N -> slide N-k」的位移從某頁起整體 +1，那一頁就是影片沒播到的。實例：某章講義有一張 Acknowledgement 頁老師直接跳過，講義 28 頁對影片 27 張。

⚠️ **投影片編號以影片上印的為準**，並在筆記表頭註明與講義 PDF 的落差。

### 步驟 3　轉逐字稿（asr）

**模型與裝置自動選**：有 CUDA 就用 **large-v2 / GPU / float16**，沒有就退回 **medium / CPU / int8**。`--device cpu|cuda` 可強制。

實測（5 分鐘課程音檔，誤聽詞數越少越準；誤聽詞庫來自既有校正表）：

| 設定 | 耗時 | 相對實時 | 誤聽 |
|---|---|---|---|
| CPU medium int8 | 252 s | 1.2× | 22 處 |
| GPU medium fp16 | 29 s | 10.3× | 23 處 |
| **GPU large-v2 fp16** | **51 s** | **5.9×** | **10 處** |
| GPU large-v3 fp16 | 59 s | 5.1× | 18 處 |

large-v2 同時是最準的和比 CPU 快 5 倍的選擇。60 分鐘的影片約 **20 分鐘**轉完（CH2 全長 59:17 實測 12 段共 1175 s）。

**large-v2 vs CPU medium 的實測差異**（CH2 全長 59:17，同一支音檔）：內容準確度明顯較好——medium 把「電流公式」聽成「電流共識」、「forward bias」聽成「Four-Five」、「doping」聽成「兜屏」、「電子濃度梯度」聽成「電池的濃度T2」，large-v2 這些都對。共用校正表在 large-v2 的輸出上命中 100 種、246 處。

**分段可續跑**：切成 `--chunk` 分鐘一段（預設 **5**），每段轉完立刻落檔到 `AI/CH<N>/_chunks/partNN.json`，被中止只損失當前那一段。相鄰段有 15 秒重疊，避免跨切點的字被切一半。

⚠️ **`--chunk` 不要調大。** large-v2 餵超過 5 分鐘的 clip 會在後半段崩成碎片：實測同一段 CH2 音檔，10 分鐘的 chunk 讓段長中位數掉到 **1.50 s**（106/209 段 ≤1.5 s，500~600 s 區間平均只剩 5.2 字/段，一句話被切成 5~8 段），切 5 分鐘則三段全正常（中位數 4.00 s、≤1.5 s 只有 12 段、平均 19.2 字）。這是 `condition_on_previous_text` 在長 clip 上累積退化，跟 GPU 無關。**分段變多不會變慢**（總耗時幾乎相同）。

**兩段之間的交界由程式處理，不要自己動**（`_dedup_boundary()`）：
- 重疊區的過濾用**段落的 end** 不是 start。用 start 會把「start 在重疊區、end 跨過邊界」的長段整段丟掉，而前一段的 clip 切到邊界就停了也轉不到它 → 那幾秒**靜默消失**（實測 CH2 的 599.4~610.0 s 被吃掉 10.6 秒，舊版有兩句話）。
- 前一段末尾常留下被 clip 截斷的殘段（「我們會發現」），與下一段的完整句重疊 → 用**開頭字串相似度 ≥0.8** 判定並丟棄。不能用嚴格前綴：兩段是各自轉錄的，同一句的誤聽字未必相同（實測一邊「空閥區」、一邊「空滑區」）。
- 合併後會自動報 **>8 秒的時間空白**。多半是老師換頁的停頓（對照 `slides.json` 的換頁時間就知道），真漏字才要回頭查。
- 判不出誰是殘段的（whisper 偶爾在同一段裡自己給出重疊的時間戳），兩段都留著並寫進 `AI/CH<N>/_overlaps.txt`。**讀逐字稿做人工斷句時對著它看一眼**，該刪的順手處理掉。CH2 全長實測 7 處。

**large-v2 在講話連續的段落會把句子切得很碎，這是正常的。** CH2 全長實測：前 20 分鐘中位段長 3.8~5.0 s，20~50 分鐘掉到 1.0~2.0 s（38% 的段落 ≤1.5 s），因為老師那段講話幾乎不停頓。**不影響最終筆記**——`build` 會依 breaks.json（或機械規則）把它們合併成 3~6 行的段落，實測 40 分鐘處的頁面版面完全正常。只是讀逐字稿時會比較累，要有心理準備。**不要為了這件事去調轉錄參數**，見步驟 5。

### ⚠️ 轉錄完一定要先看空白統計，再往下做

`asr` 跑完會印 `>8 秒空白` 的清單。**空白多不等於老師沒講話** —— 2026-09-11 實測 DIC 那支
2 小時的影片，`condition_on_previous_text=True` 讓 **22.3%（1625 秒 / 27 分鐘）完全沒轉到**，
而那些時段的音量與正常時段相同（mean −27 dB / max −4 dB，不是靜音）：模型卡在重複迴圈，
每 30 秒吐一句一模一樣的話然後跳過那個視窗。

**判法**：挑兩三個空白時段跑 `ffmpeg -ss <t> -t 30 -i CH<N>.wav -af volumedetect -f null -`，
音量跟正常時段一樣就是模型的問題。

**解法**：`asr` 加 `--no-condition` 重跑（`_chunks` 要先移走，不然會跳過已完成的段）。
實測空白 83 處 / 1625 秒 → 12 處 / 170 秒，字數 +35%。代價是句子被切碎、句尾標點掉到 1%，
但那本來就是 `breaks.json` 要處理的；漏掉的 27 分鐘救不回來。
`vad_filter`、`no_speech_threshold`、`log_prob_threshold` 對這個症狀**完全沒有影響**（輸出逐字相同），
不用再試。

剩下的空白幾乎全部落在 5 分鐘 chunk 的交界（`_dedup_boundary()` 砍過頭），
對每個空白單獨切一段重轉、只收「中點落在空白區內」的段落就能補回來。
（課程專屬腳本 `patch_gaps.py`，未收錄於本 repo。）兩件事要注意：

- **模型會把 `initial_prompt` 本身當成語音吐出來**（實測補回「一段 437 字」就是 prompt 的
  專有名詞清單）。判準用「與 prompt 的重疊 ≥40 字」，**不可以設 15 字**——
  「threshold voltage」本身就 17 字，老師講到這個詞的正常句子會整句被誤殺。
- 補不到的窗口**第二輪改成不給 prompt**，實測 4 個空白全部補回真內容。
- 算「字/秒」一律用**顯示寬度**（全形 1、半形 0.5），不可以用 `len()`——
  英文專有名詞的字元數是中文的兩倍，用中文語速門檻會誤刪「Metal Oxide Semiconductor」這種正常段落。
  而且這個過濾器**只准套用在新補回的段落上**，不要拿去掃整份 `transcript.json`。

參數是刻意的，**不要改**：

```python
vad_filter=False, condition_on_previous_text=True,
repetition_penalty=1.15, no_repeat_ngram_size=4,
compression_ratio_threshold=2.2, prompt_reset_on_temperature=0.3
```

- 前兩項讓標點與段落長度變好（實測 5 分鐘片段：句尾標點率 5% → 79%）。
- 後四項專門壓制 `condition_on_previous_text` 的重複迴圈（曾出現同一句重複 5 次）。

⚠️ **不要把前一段的逐字稿接成下一段的 prompt。** 實測誤差會滾雪球：只給領域提示的 part00 標點率 98%，接了前文的 part01 起一路掉到 0%。每段都只給領域提示。

### 步驟 4　校正專有名詞（fix）

ASR 對技術名詞誤聽極多（59 分鐘的課實測 415 處）。校正表分兩層：

- `scripts/corrections_common.json`——跨章通用（空乏區、電洞、偏壓、摻雜濃度…），目前 **144 條**。**新章發現通用誤聽就加進去**，是會累積的資產。加的時候只收「錯字本身不是有效中文詞」的高信心條目（`電芯體`→`電晶體` 收，判不準的 `Clear Current` 不收），判不準的留給該章自己的 `corrections.json`。
- `AI/CH<N>/corrections.json`——本章專屬（人名、該章特有術語）。

作法：讀 `transcript.txt` 全文，把誤聽整理成 `[["錯","對"], ...]`，長詞放前面。跑完會印**未命中的條目**，用來抓拼法不同的漏網之魚。原始轉錄保留在 `transcript_raw.json`，`fix` 可重複執行。

**聽不清楚或與投影片不符的地方用〔方括號〕標出來，不要自己編。**

**跑完 fix 之後，一定要再逐句讀一遍 `transcript.txt` 做語意校對**（2026-09-18 使用者裁示：「不要太多錯，我一邊看一邊改很花時間」）。
AIC CH2 交付後使用者邊看影片邊抓出十幾處，回頭逐句讀又找到約 80 處，全是 fix 那一輪漏掉的：

- **同音異字、本身是合法中文詞**，校正表的「只收非有效詞」原則抓不到：`電廠`→`電場`、`代電量`→`帶電量`、`共識`→`公式`、`焦點`→`交點`、`放大期`→`放大器`、`裡程碑`→`里程碑`。
- **一個字聽錯**：`P應區`／`P行區`／`N進區`／`P0區`→`P型區`、`輸入到 Base`→`注入到 Base`、`隨時增加`→`隨之增加`。
- **英文聽成怪字**：`Aption`→`ε`、`Base Client`→`Base Current`、`Affected Width`→`Effective Width`、`reverse biaste`、`Minivolt Producee`→`millivolt per degree`。
- **下標符號聽錯**：`VV`／`VB`→`VBE`、`Iv`→`IB`、`RV`→`rb`——要對照投影片上實際有哪些符號。
- **術語要看語意判斷，不是一律取代**：「PN 界面」一定是「PN 接面」，但單獨的「界面」不一定錯；「截面積」不是「結面積」。

判斷依據是**該頁投影片＋前後句的物理意義**，不是字面。老師本人的口誤（例如把 Cμ 說成 diffusion capacitance）**不改逐字稿**，在重點拆解裡指出即可。
改完的修正也要補進本章 `corrections.json`（帶前後文，避免誤傷其他句子），重跑 fix 才不會被蓋回去。

### 步驟 5　人工審定段落斷點（breaks.json）— **這一步不能跳**

**最重要的一條教訓：逐字稿沒有句尾標點時，任何機械規則都會把句子從中間切開。**

實測過三種規則全部失敗：
- 靠句尾標點 → 這類長時間講課只有 1.6% 的段落有句號
- 靠說話停頓 → 停頓 ≥0.3 秒只有 71 個斷點（平均 244 字一段，比不切還糟）
- 靠語氣詞（那、所以、接下來…）→ 79% 命中，但剩下的 21% 會切出「…平行板電極的右邊 ‖ 有很多帶正電的離子存在」這種明顯的半句

而且**標點率取決於老師那一段的講話方式**（同一支影片前 10 分鐘 98%、20 分鐘後 1~2%），換模型參數、切短片段都救不回來（3 分鐘 2%、5 分鐘 1%、10 分鐘 10%）。**不要再嘗試用轉錄參數解決這件事。**

正解是：**產重點拆解時本來就要通讀逐字稿，順手把段落起點記下來。**

⏱ **這三件事要在同一次閱讀完成**：校正表（步驟 4）、斷點（步驟 5）、重點拆解（步驟 6）
都需要通讀同一份逐字稿。分三次讀會白白多花 20~30 分鐘——讀一遍，三樣一起產出。

```
python -c "import json; t=json.load(open('AI/CH<N>/transcript.json',encoding='utf-8'))
for i,r in enumerate(t): print('%4d [%02d:%02d] %s' % (i,int(r['start'])//60,int(r['start'])%60,r['text']))"
```

讀完後寫 `AI/CH<N>/breaks.json`：

```json
{ "reviewed_slides": [1,2,3], "starts": [1, 7, 9, 12, 14, 15, 24, 30, ...] }
```

`starts` 是**全域段索引**，每個代表一個新段落的開始。`build` 有 breaks.json 就照人工斷點切，沒審過的範圍退回 `note_utils.py` 的機械規則。

**斷點選在「一個意思講完」的地方**，不要為了湊長度硬切。長度由步驟 6 的平衡處理收尾。

🚨 **不可以「刪掉」斷點。** 2026-09-11 踩過：把切在半句的斷點挑出來想挪位置，
有 15 個找不到合適的新位置就直接刪了——**刪一個斷點等於把兩段黏成一段**，
結果產生 15 個 >8 行、最長 **15.9 行**的字牆（使用者回報「一次飆十幾行」）。
寧可切在稍微不漂亮的地方，也不要留字牆。

**交付前一定要量段落長度**，這是機械可驗的，不要靠翻一翻看看：

```python
disp = lambda t: sum(1.0 if ord(c) > 0x2000 else 0.5 for c in t)   # 全形 1、半形 0.5
rows = [disp(re.sub(r'<[^>]+>', '', m.group(1)))/25
        for m in re.finditer(r'</b> (.*?)</p>', md)]
# 驗收條件：>6.4 行的必須是 0；中位數落在 4~5
```

🚨 **超標的段落一律手動在句尾補斷點，禁止用腳本自動補**（2026-09-19 使用者裁示）。
舊做法 `split_long.py`（評分「語氣詞開頭 > 停頓長 > 靠近中點」）不讀語意，AIC CH3 用它補的 20 個斷點裡
切出「稱為叫做 ‖ Gate Drive Voltage」這種半句——它是整條流程裡唯一不讀語意就寫入斷點的環節。
做法：列出超標段落 → 讀那一段 → 在「一句講完」的位置切（老師 7 行的段落通常有 2~3 句）。
切完的短段會被 `balance()` 併回鄰段，所以要切到**併完仍 ≤6.4 行**，不夠就再切一次。
`split_long.py` 只准拿來**列出**超標段落，不准寫入 breaks.json。

#### 頁界檢查：上一頁的句尾不能跑到下一頁開頭（2026-09-18 使用者裁示）

`build` 預設用「換頁時間 − 6 秒」決定每段歸哪一頁。但老師**常常話還沒講完就換頁**，
一句話的後半截就被切到下一頁開頭。AIC CH2 27 頁有 **12 處**，例如：

| 下一頁開頭（錯） | 其實是上一頁的結尾 |
|---|---|
| 「所以當你的二極體在做切換的時候呢，它會有一個比較快的速度」 | p.8 講蕭特基二極體的結論 |
| 「就會構成另外一個 Base Current 的成分」 | p.11「你所補充的這些電洞的電流」的後半句 |
| 「都會隨之增加」 | p.15「IC 跟 IB 的比例」的述語 |
| 「所謂小信號電阻」 | p.23「人為創造出來的」的受詞 |

**判斷要看語意，不是看時間**（使用者原話：「你要了解語意，才會知道不應該那樣放」）。
每一個頁界都要讀「上一頁最後 2 段＋下一頁最前 3 段」，問兩件事：
1. 上一頁的最後一句**講完了嗎**？（缺受詞、缺述語、停在「的時候」「的話」都是沒講完）
2. 下一頁的第一段是**接著上一句講**，還是**開新話題**？開新話題通常是「這一頁我們要介紹…」「接下來…」「針對前面的討論…」。
   以「所以」「就」「都」「使得」「因為這個原因」開頭、或整段沒有主詞的，多半是接上一句。

接上一句的，在 `breaks.json` 寫 `page_start`，指定下一頁從哪一段開始（全域段索引）：

```json
{ "starts": [...], "page_start": {"9": 267, "12": 393} }
```

- `page_start` 優先於換頁時間；沒寫的頁維持時間規則。必須單調遞增，`build` 會 assert。
- 被拉回上一頁的段落索引要從 `starts` 拿掉，不然它會在上一頁自成一段。
- 檢查腳本：列出每個頁界的前後段落（`AI/CH<N>/transcript.json` + `slides.json`，段落的 `start >= sec-6` 歸下一頁），逐一判讀，不要只看有沒有句號。

### 步驟 6　寫 content.json 並產出筆記（build）

`AI/CH<N>/content.json`：

```json
{
  "course": "類比積體電路 (AIC)",
  "course_short": "AIC",
  "course_link": "類比積體電路(AIC)",
  "teacher": "陳巍仁",
  "title": "PN Junctions and BJT",
  "slug": "PN_Junctions_and_BJT",
  "source": "AIC2025Spring CH2.PN BJT（59:17，E3 OCW）",
  "duration": "59:17",
  "slides": [ {"n": 1, "heading": "課程標題頁", "explain": "markdown 內容…"} ]
}
```

用 `.py` 產生這個 json（三引號字串寫 markdown），**不要手打 JSON**——跳脫很容易出錯。

`build` 會自動做兩件事：
- **段落結尾補句號**（Whisper 常常不給），逗號類先去掉再補。
- **長度平衡**（`vts.py` 的 `balance()`）：不足 3 行的段落併進**較短的鄰段**，前提是併完不超過 6.4 行。**只做合併、不新增斷點**，所以不會破壞語意邊界。參數在 `vts.py` 頂端：`MIN_LINES=3, MAX_LINES=6.4, CPL=25`。

目標是 **3~6 行**，主群落在 4~5 行。剩下的 1~2 行例外通常是「該頁只有一個段落」或「兩邊鄰段都已 4~5 行」，屬於不得已。超過 6 行的要手動在 breaks.json 補一個斷點切開。

⚠️ **行數要用顯示寬度算，不是字數**：半形英數只佔全形字一半寬。`disp_lines()` 已經處理，不要自己用「字數 ÷ 25」估——會嚴重高估夾雜英文的段落。

**寫 explain 的原則**：
- 投影片上寫了什麼不必重複，重點是**老師口述補充了什麼**、**這頁跟前後頁怎麼串**、**跟課程進度表哪一週對應**。
- 老師點名「這個比較難」「這要靠模擬」「這是代價」的地方一定要標出來。
- 表格用來壓縮對照類內容（參數總表、新舊製程比較、週次對應）。
- 發現投影片本身有錯（打錯名字、量綱不對）要指出來。

`build` 會做四項自檢：章節數、版面區無空行、每節結構、逐字稿段落全數分配。任何一項失敗就中止不寫檔。

### 步驟 7　渲染確認版面（preview）

用 headless Chrome 把筆記渲染成 PNG，分批輸出到 `AI/CH<N>/preview/`。**使用者在外面時用這個給他看**，不要叫他開 .md。

🚨 **預覽的寬度、字級、行高必須跟他的 Obsidian 一樣，不然沒有意義。**
2026-09-11 被指出過：預覽用 1100px 正文寬，他的 Obsidian 是**預設的 700px**
（「可讀行長」預設開啟，`--file-line-width: 700px`），逐字稿的換行位置完全對不上，
拿來驗版面等於白驗。`CSS` 常數與 `--window-size` 已經照預設值設好：

**不要從設定檔去推寬度，要請使用者當場量。** 我推了三次都錯：
① `app.json` 沒有 `readableLineLength` → 猜預設 700px（每行比他少 6 字）；
② 發現 `ESD-fundamental.css` 有一條沒限定 class 的 `max-width:960px` → 猜 960（多 5 字）；
③ 實際量出來是 **854px**——960 扣掉 Obsidian sizer 自己的左右留白。
中間還有 `--zoom-factor: 1.31`、UI 字級 15px vs 正文 16px 這些干擾項。

**量法**（請使用者做，30 秒）：Obsidian 按 `Ctrl+Shift+I` →
**先把 DevTools 用 ⋮ → Dock side 停到最下面**（停在右邊會把筆記擠窄，量到的是假的，
第一次就是這樣量到 431px）→ 切到 `Console` 貼這行，結果會複製進剪貼簿：

```js
const v=document.querySelector('.markdown-preview-view'),r=document.querySelector('.markdown-preview-view div[style*="display:flex"]');copy([Math.round(r.getBoundingClientRect().width),getComputedStyle(v).fontSize,getComputedStyle(document.querySelector('.markdown-preview-view p[style*="0.92em"]')).fontSize].join(' | '))
```

⚠️ 選擇器要鎖在 `.markdown-preview-view` 裡面。量 `document.body` 的字級會拿到
**側邊欄的 UI 字級 15px**，不是筆記正文的 16px。

這個 vault 的實測值（2026-09-11）：**正文列寬 854px、正文 16px、逐字稿 14.72px**，
已設成 `preview` 的預設；換機器或改設定就用 `--width` / `--font` 覆寫。
`--force-device-scale-factor=2` 只影響 PNG 清晰度，不影響版面。
對準之後逐字稿的換行位置與他的畫面差在 1 個字以內（字體 fallback 的差異，無法再縮小）。

## 版面規格（已定案，不要自己改）

每頁三段：投影片（左 44%）＋逐字稿捲動框（右）→ 重點拆解（下方全寬）→ 分隔線。

**四條硬規則**（每一條都是踩過才知道的）：

1. **全部用行內樣式，不要用 `<style>` 區塊**——Obsidian 閱讀模式不套用筆記內的 `<style>`，flex 會整個失效、圖片變全寬。
2. **HTML 區塊內不得有空行**——Obsidian 的 HTML 區塊遇到空行就結束，表格／div 會從中間斷掉。
3. **HTML 區塊內不解析 Markdown**——所以上半部那一列（投影片＋逐字稿）內容一律直接寫 HTML。
4. **逐字稿面板用絕對定位填滿整列**（`position:absolute` + inset 0），高度就自動等於投影片高度、任何視窗寬度都對齊；面板內部用 flex 直向排，捲動區吃掉剩餘高度。
5. **重點拆解是純 Markdown，不要包進 `<div>`**（2026-09-11 改）——包進去 Obsidian 就不跑 MathJax，
   數學式渲染不出來。改成純 Markdown 之後表格、粗體、清單、數學式全部由 Obsidian 自己渲染。

### 英文一律用 Times New Roman（2026-09-11 使用者定案）

筆記裡的英文（含標題、`term`、`source`／`drain` 這些穿插在中文句子裡的字）一律襯線體，
跟重點拆解的 MathJax、逐字稿的 `.tx` 是同一個家族，整份讀起來才協調。

**作法是一行 CSS，不要去改筆記內容、也不要標記哪些字是英文**：

```css
.ocw-study {
  font-family: "Times New Roman", "Microsoft JhengHei", "Noto Sans TC", sans-serif;
}
```

原理是**字體 fallback 逐字生效**：Times New Roman 沒有任何中文字形，所以把它排在中文字體
**前面**，英數命中它、中文自動落到後面的微軟正黑。

- 位置：`<vault>\.obsidian\snippets\` 底下已啟用的 CSS snippet 的 `.ocw-study` 區段；
  `preview` 的 `CSS` 常數要同步（兩邊都改，否則預覽跟 Obsidian 不一樣）。
- 不受影響的三處（各自帶字體）：重點拆解的 MathJax、逐字稿符號的 `.tx`、投影片圖。
- ⚠️ **沒辦法只套某一頁**——Obsidian 閱讀模式沒有把每個章節包成可選取的區塊，
  CSS 選不到「p.7 到 p.8 之間」。要給使用者看單頁效果，就整份套上去再渲染那一頁的預覽，
  並告訴他「不喜歡刪一段就還原」。

**配套：中文與英數之間要插細空白。** 字體換成襯線之後，逐字稿裡大量的中英夾雜
（「形成 channel」「等於 0 的時候」）會擠在一起。CSS 的 `text-autospace` 還不能用，
所以在**文字層**插入 U+2009（thin space，約 1/5 em）——`build` 的 `cjk_latin_space()` 已處理。

- 只處理 `<span class="tx">` **以外**的部分：符號本身已經有 margin，再插細空白會變兩倍寬。
- 不碰全形標點（`，。「」`）：那些字身本來就留了空隙，「講過，OK」不需要再加。
- 驗法：`grep -c '<span class="tx">[^<]* '` 必須是 0。
- 逐字稿**已經人工校正過、本來就有空格**的章節會是 0 個細空白（AIC CH1 就是），
  那不是壞掉。判準是先數「中英直接相鄰」有幾處。

### 重點拆解要左右對齊（2026-09-11 使用者裁示）

中文段落預設是靠左，行尾常常留一大塊白才換行。**重點拆解的內文一律 justify。**

作法：**用 Obsidian 的 CSS snippet，不要包 HTML**（包進去就沒有 MathJax，見硬規則 5）。

- 筆記 frontmatter 寫 `cssclasses: [ocw-study]`（`build` 已自動加）。
- 樣式寫在 `<vault>\.obsidian\snippets\` 底下已啟用的 CSS snippet 最底下的 `.ocw-study` 區段。
  **那個檔已經在 `appearance.json` 的啟用清單裡，所以不必叫使用者去開開關，也不要另建新檔**
  （另建新檔要改 appearance.json，而且那個檔的抬頭明講不要再開一份）。
- ⚠️ **三種模式的 DOM 不一樣，選擇器要全部列出來**。第一版只寫了 `p` 和 `li`，
  使用者在**即時預覽**下打開完全沒作用——那個模式的 Markdown 段落根本不是 `<p>`，
  是 `div.cm-line`。三種都要寫：
  `.ocw-study p` / `.ocw-study li`（閱讀模式與匯出 PDF）、`.ocw-study .cm-line`（即時預覽）。
  主題可能本來就設了 `text-align`，所以加 `!important`。
- 只 justify 重點拆解：**逐字稿捲動框維持靠左**，那欄很窄，對齊只會擠出更大的字距。
  用 `p[style*="0.92em"]` 認出逐字稿段落（`P_STYLE` 寫死的字級）把它排除掉。
  標題與表格儲存格也要排除（單行，對齊只會讓字距忽寬忽窄）。
- 🚨 **explain 的原始碼不可以有硬換行——這是對齊失效的真正原因，CSS 只是表象。**
  **Obsidian 預設「嚴格換行」是關的，原始碼裡的單一換行會渲染成真正的 `<br>`**，
  而 `text-align: justify` **永遠不會對齊「換行前的最後一行」**。
  於是每一行都成了自己的最後一行，整段看起來完全沒有對齊。
  `build` 的 `unwrap()` 會把同一段的軟換行攤平成一行（表格、清單、引用、標題保留原樣），
  所以寫 explain 的時候可以照自己舒服的寬度折行，但**不要繞過 `build` 直接寫進 .md**。
- **改完要請使用者實際在 Obsidian 看一眼再宣告完成**——預覽 PNG 走的是 headless Chrome
  ＋ python-markdown，**它把單一換行當成空白**，跟 Obsidian 完全相反，
  所以上面那個 `<br>` 問題在預覽裡**看不出來**（預覽是對齊的、Obsidian 不是）。
- 分不出「class 沒對上」還是「屬性沒生效」的時候，**加一條一眼看得出來的診斷規則**
  （例如 `background: #fff6c2`）請使用者回報，一次就定位，不要憑猜的改第二輪。
- `text-justify: inter-ideograph`（調中文字距，不是把英文單字拉開）＋
  `hanging-punctuation: allow-end`（標點掛行末）。
- `preview` 的 `CSS` 常數裡有一份等價規則，**兩邊要一起改**，否則預覽 PNG 跟 Obsidian 不一樣。

### 逐字稿面板的上下標用 `<sub>`，不是 LaTeX（2026-09-11 使用者裁示）

逐字稿那一列是 **raw HTML**（捲動框非它不可），而 **Obsidian 在 HTML 區塊裡不跑 MathJax**，
所以那裡寫 `$V_{GS}$` 只會原樣顯示 `$V_{GS}$`。改用 HTML 自己的 `<sub>`：
`VGS` → `<span class="tx">V<sub>GS</sub></span>`，`build` 的 `html_subscripts()` 已處理。

**看起來仍然像 LaTeX**：`.tx` 在 CSS 裡套 `Cambria Math`（Windows 內建，字形接近
Computer Modern）＋斜體，跟下方重點拆解真正的 MathJax 擺在一起不違和。
`sub` 另外設 `line-height:0` + `position:relative`，**否則會把行高撐開**——
捲動框的高度是跟著投影片固定的，行高一變整段就錯位。

🚨 **白名單必須分課，不可能有一張通用的**：

| 字串 | 在 DIC | 在 AIC 的 BJT 章 |
|---|---|---|
| `IC` | 15 次**全部**是「IC 設計」（integrated circuit） | 25 次**全部**是集極電流 |

所以通用白名單（`vts.py` 的 `SUBS`）只收兩門課都不會誤判的（VGS、VDS、VDD、VTH、Cox…），
該門課才成立的寫在 **`content.json` 的 `subs_extra`**（AIC 是 IC、IB、IE、VA、VB、RB…）。
**加字進白名單前一定要 grep 那門課的 transcript 看前後文**，不要憑印象——
`IC`、`ID`、`IB` 在口語裡太容易誤中。

另外兩件實際回報過的事：

- **大小寫要一起收、並統一成正規寫法。** ASR 的大小寫很隨機，同一句會出現
  「你 Vg 跟 vs 之間的電位差」。白名單要連 `vgs`、`vds`、`vt`、`Ic` 這些變體一起列，
  `SUBS_CANON` 再把電壓符號統一成大寫（**只統一 V 開頭的**——`gm`、`rex`、`Cox`
  本來就該小寫，不可以一律 `upper()`）。
- **`.tx` 的左右邊距要不對稱，`margin-inline` 一個值不夠。**
  數學斜體往右上倒、下標又掛在右下角，**右側的視覺空隙會被字形本身吃掉**。
  左右都給 `0.12em` 的結果是「左邊剛好、右邊還是黏著中文」（使用者兩次都指出右邊）。
  實際值：`margin-left: .08em; margin-right: .26em;`。

驗法：`grep -o '[A-Za-z]<span class="tx">'` 必須是零（符號黏在英文字裡就是誤判）。

**ASR 把符號聽成怪東西的，要走校正表不是白名單。** 實例：
`VGS 我們就把它視為是VG-S VS` —— 老師說的是「V_G 減 V_S」，
whisper 聽成 `VG-S VS`。這種要在 `corrections.json` 補一條，白名單救不了。

### 算式與上下標一律用 LaTeX（2026-09-11 使用者裁示）

重點拆解裡**不要**寫 `V_GS`、`W/L`、`I_DS` 這種純文字上下標，一律寫成 `$V_{GS}$`、`$W/L$`、`$I_{DS}$`。
整條式子就整條包起來：`$(V_{GS}-V_t)^2$`、`$v = \mu \times dV/dx$`、`$I_{DS} \propto V_{DS}$`。

- **這條只管 `.md` 產物**。終端機回覆仍然禁用 LaTeX 與 `<sub>`。
**轉換器是 skill 自己的兩支腳本，所有課共用，不要每門課各寫一份**：

| 腳本 | 管什麼 | 例 |
|---|---|---|
| `scripts/tex.py` | 散在中文句子裡的**單一符號** | `V_GS` → `$V_{GS}$`、`Cox` → `$C_{ox}$` |
| `scripts/formula.py` | **整行都是算式**的行，整行包起來 | `IC = Is·e^(VBE/UT) = βF·IB` → `$I_C = I_s\cdot e^{...}$` |

呼叫順序**先 formula 再 tex**：反過來的話整行已經被塞滿 `$`，formula 就判不出它本來是一條式子。
每門課專屬的片語用 `tex.convert(s, extra)` 的 `extra` 傳進去
（範例：DIC 的 `make_content.py`、AIC 的 `AI/scripts/aic_latex.py`）。

⚠️ 已經踩過的五個坑，改這兩支腳本前先看：

1. **先換成佔位符、全部換完再展開。** 一輪輪 `replace` 會改到自己剛產出的東西：
   `VDD − V_t` 先變成 `$V_{DD}-V_t$`，後面那條 `V_t -> $V_t$` 又打進去，
   結果是 `$V_{DD}-$V_t$$`。
2. **每門課專屬的片語要在「保護 code span」之前做**——它們多半本來就寫成
   `` `I_DS ∝ V_DS` ``，先保護就等於鎖住，永遠換不到。
3. **比對 LaTeX 指令一定要用指令清單、長的排前面，不可以寫 `\\[a-zA-Z]+`。**
   那種寫法會回溯：`\beta_{F}` 裡 `[a-zA-Z]+` 先吃掉 `beta`、發現後面不是字母，
   退回成 `bet`，再看到 `a` 是字母就插空白 → `\bet a`。
4. **片語要卡邊界。** 沒卡的時候 `(W/LpE)` 裡的 `W/L` 被換掉，變成 `($W/L$pE)`。
5. **式子裡的運算子要用 ASCII**（`-`、`>`、`\ge`），U+2212 那個全形減號 KaTeX 吃不下。
- 驗法：把 `$...$` 全部挖掉之後再 grep `[A-Za-z]_[A-Za-z0-9]`，**必須是零**；
  順便數 `$` 的總數是不是偶數。

**包進 `$` 不等於寫對了——式子本身也要是像樣的 LaTeX**（2026-09-18 使用者裁示：「`e^` 這種怪怪的打法我不要看到」）。
AIC CH2 轉完之後還留著 18 處，兩類：

1. **轉換器沒碰到的**：`**` 粗體包住的整行式子（`> **iC = Is·e^(vBE/VT)**`）與**表格格子**裡的式子，formula.py 判不出來，原封不動留著 `e^(`、`·`、`τT`。
2. **包了但沒改寫的**：`$I_{s}\cdot e^{VBE/UT}$`——指數裡的 `VBE/UT` 沒下標、`\cdot` 滿地、分數用斜線。

寫法標準（寫 explain 時就直接照這樣寫，不要靠轉換器事後補）：
- 指數裡的符號一樣要下標：`e^{V_{BE}/U_T}`，不是 `e^{VBE/UT}`。
- 乘號省略或用 `\,`，不要每個乘號都寫 `\cdot`；分數用 `\dfrac{}{}`（行內）或 `\frac{}{}`（`$$` 區塊內），不要寫 `a/(b·c)`。
- 推導要分步寫，每步一條 `$$...$$` 獨立一行；推導類的式子不要擠在 `>` 引用區塊的一行裡。
- 表格格子裡的式子也要包 `$`，但 `$...$` 裡不可以出現 `|`（會切斷表格），平行符號寫 `\parallel`。

驗法（補在上一條驗法後面，三項都**必須是零**）：
- 挖掉逐字稿面板（`<p style=...>` 那些行）後 grep `e\^\(`。
- grep `e\^\{[A-Za-z]{2,}`——指數裡有連續兩個以上沒下標的字母。
- 挖掉 `$...$` 後 grep `[A-Za-z]·[A-Za-z]` 與 `τ[A-Za-z]`（`τT`、`τF` 這類沒轉的希臘字母＋下標）。
- `preview` 已經掛了 KaTeX（CDN）並加了 `--virtual-time-budget=8000` 等它渲染完，
  所以預覽 PNG 看到的就是渲染後的式子。連不到 CDN 只會退回顯示原始碼，不會失敗。

## 收尾

1. **把筆記掛回課程頁**：課程 hub 的章節表加一列 `[[<檔名>]]`。筆記底部已自動有「回上層」，但單向連結從 hub 找不到，兩邊都要有。
2. **副產物留在 `AI/`**（逐字稿、投影片、mp4、_chunks、preview）——`AI\` 是 Obsidian graph 的有效過濾位置。
3. **提醒影片下架風險**：OCW 影片常常釋出後數週下架，一釋出就要下載。

## 不要做的事

- 不要用講義 PDF 當投影片圖源（黑白、且編號可能與影片不符）。
- 不要把 `--shots` 關掉或調到 1（雷射筆會入鏡），也不要自己改雷射點的判準——步驟 1 列了兩個試過失敗的方向。
- 不要改 ASR 那六個參數，也不要把前一段逐字稿接成下一段的 prompt。
- 不要把 `--chunk` 調大（>5 分鐘 large-v2 會碎片化），也不要自己改 chunk 交界的過濾邏輯。
- 腳本裡要 `log()` 出來的訊息不要用 emoji 或 `⚠`：主控台是 CP950，編不出來會丟 `UnicodeEncodeError` 把整支轉錄打斷（`log()` 已加防護，但訊息本身用純中文最保險）。
- **不要試圖用轉錄參數或機械規則解決斷句問題**——已經證實無解，老實讀完逐字稿人工斷。
- 不要在筆記裡用 `<style>` 區塊或在 HTML 區塊裡留空行。
- 不要自己編轉錄聽不清楚的內容，用〔〕標出來。
- 不要把副產物放進 `AI/` 以外的地方。
- 改腳本用 Edit 工具，不要用 heredoc——`\n` 會被摺半成真正的換行，造成語法錯誤。
